extends base

block content
    div#videoContainer(data-id=video._id)
        video(src="/" + video.fileUrl)
        div#videoControls.videoControls
            div.videoControls__play
                span#play.videoControlsplayBtn
                    i.fas.fa-play
                div.videoControls__time
                    span#currenTime 00:00
                    span  / 
                    span#totalTime 00:00
            input(type="range",step="1", value="0", min="0")#timeline.videoControls__timeline
            div.videoControls__volume
                input(type="range",step="0.1", value=0.5, min="0", max="1")#volume
                span#mute
                    i.fas.fa-volume-up
            div
                span#fullScreen
                    i.fas.fa-expand
    div.video__data
        p.video__title=video.title
        small.video__owner Uploaded by 
            a(href=`/users/${video.owner._id}`)=video.owner.name
        small.video__createdAt=new Date(video.createdAt).toLocaleDateString("ko-kr", {weekday: 'long', year: 'numeric', month: 'long', day: 'numeric'})
        if String(video.owner._id) === String(loggedInUser._id)
            a(href=`${video.id}/edit`) Edit Video &rarr;
            br
            a(href=`${video.id}/delete`) Delete Video &rarr;
    if loggedIn
        div.video__add-comments
            form.video__comment-form#commentForm
                textarea(cols="30", rows="10", placeholder="Write a nice commment...")
                button Add Comment
    div.video__comments
        ul
            each comment in video.comments.reverse()
                li.video__comment(data-id=comment.id)
                    i.fas.fa-comment
                    span #{comment.text}
                    span  ❌
                    //- x를 클릭하면 fetch request를 보내서 댓글을 지우게 한다.
                    //- = API route와 controllers를 만들어야 한다.
                    //- = 그 유저가 댓글의 주인이 맞는지 댓글 삭제 전에 확인해야 한다.
                    //- 어쩌면 x 버튼이 모두에게 보여서는 안될 수도 있다 (댓글 작성자에게만 보이기)
                    //- -> 이거는 watch.pug line28에서 edit, delete vid 버튼 주인만 보이는 것 참고
                    //- -> 백엔드에서도 버튼을 클릭하고 fetch requiest 보내는 사람이 댓글 작성자가 맞는지 확인 필요
                    //- fetch를 사용하기 때문에 get, post 외에도 다른 많은 걸 보낼 수 있음. (ex. method: "DELETE")
                    //- 그러면 router 안에서  router.post/get 대신에 delete 사용 가능 (ex. videoController.js ->   r.delete)
                    //- 암튼 결론은 x 버튼을 모두에게 보여주지 말고, 작성자에게만 보여지게 평소에는 html에서 숨겨줄 것!

block scripts
    script(src="/static/js/videoPlayer.js")
    if loggedIn
        script(src="/static/js/commentSection.js") 
    
    //- video(src="/" + video.fileUrl)  
    //- div
    //-     button#play Play
    //-     button#mute Mute
    //-     input(type="range",step="0.1", value=0.5, min="0", max="1")#volume

        div 
            span#currentTime 00:00
            span  / 
            span#totalTime 00:00
        div
            input(type="range",step="1", value="0", min="0")#timeline
            //- input(type="range", step="0.1", value="0.5", min="0", max="1")#timeline
            //- div 
            //-     button#fullScreen Enter Full Screen
        //-
            div
                p=video.description
                small=video.createdAt
            div
                small Uploaded by 
                    a(href=`/users/${video.owner._id}`)=video.owner.name
            if String(video.owner._id) === String(loggedInUser._id)
                a(href=`${video.id}/edit`) Edit Video &rarr;
                br
                a(href=`${video.id}/delete`) Delete Video &rarr;
